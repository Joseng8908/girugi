# CLAUDE.md

이 레포에서 작업할 때 항상 따라야 하는 규칙입니다.

## 프로젝트

취준생 직무체험 시뮬레이션 플랫폼의 **백엔드 API 서버**.
사용자가 제품디자이너 역할로 AI 관계자와 대화하며 산출물을 작성하는 웹 서비스. 해커톤 MVP.

시나리오 2개를 지원합니다.
- `cmf_outsourcing` — CMF 선정 · 외주업체 컨택 (관계자 4명)
- `prototype_revision` — 프로토타입 수정, 3라운드 협상 (관계자 3명)

프론트엔드는 Next.js(별도 레포)이며 **모든 세션 상태를 브라우저 LocalStorage에 저장**합니다.
따라서 이 서버는 **상태를 저장하지 않습니다(stateless)**.

구현 명세는 `BACKEND_SPEC_v4.md`, 프론트와의 계약은 `API.md` 입니다.
두 문서가 어긋나면 `API.md` 를 따릅니다.

## 절대 규칙

1. **서버는 세션 상태를 보관하지 않는다.** 요청에 필요한 모든 정보가 실려 온다.
   메모리 캐시, 세션 스토어, Redis를 도입하지 말 것.
2. **데이터베이스를 쓰지 않는다.** `session-log` 는 stdout JSON 로깅이다.
   pgx, ORM, 마이그레이션 도구를 추가하지 말 것.
3. **RAG·벡터 검색·임베딩을 구현하지 않는다.** 도메인 지식은 전부 `prompts/` 안의
   고정 텍스트다.
4. **외부 의존성을 추가하지 않는다.** Go 표준 라이브러리만 사용한다.
   라우터·미들웨어·ORM·LLM SDK 모두 불필요하다.
5. **판정과 채점은 LLM이 하지 않는다.** 점수와 카테고리 분류는 프론트에서 끝난 상태로
   전달된다. LLM은 이미 배정된 결과를 문장으로 다듬는 용도로만 쓴다.
6. **LLM 응답 파싱 실패는 반드시 폴백한다.** 500을 던지지 말 것.
   대화가 끊기면 데모가 죽는다.
7. **프롬프트는 코드가 아니라 데이터다.** 프롬프트 문자열을 Go 소스에 하드코딩하지 말고
   `prompts/` 에서 읽는다. 이 디렉토리는 AI·기획 담당이 수정한다.
8. **disclose 화이트리스트는 코드에서 실제로 필터링한다.** 프롬프트 준수만 믿지 않는다.

## 하지 말 것 (요청받아도 확인 후에)

- 인증/로그인 (MVP에 없음)
- 사용자 테이블, 세션 테이블, DB 연결
- WebSocket, SSE (요청/응답만)
- 백그라운드 워커, 큐, 스케줄러
- `fly.toml` (배포 대상 미정, Dockerfile만)
- `scenarios/*.json` 을 백엔드 코드에서 읽기 (프론트 참조용 파일이다)

## 코드 스타일

- Go 1.22+, `net/http` 메서드 패턴 라우팅 (`mux.HandleFunc("POST /api/chat", ...)`)
- 로깅은 `log/slog`. **요청 로그에 대화 내용을 찍지 않는다**
- 에러는 감싸서 반환(`fmt.Errorf("...: %w", err)`), 핸들러에서만 HTTP 상태로 변환
- 외부 호출(LLM)은 인터페이스 뒤에 두고 테스트에서 Fake로 교체
- 주석은 왜(why)를 적고 무엇(what)은 코드로 드러낸다

## 검증

- 새 핸들러를 만들면 Fake LLM 클라이언트를 쓰는 테이블 테스트를 함께 작성한다
- `go vet ./...` 와 `go test ./...` 가 통과해야 커밋한다
- **실제 LLM API를 호출하는 테스트는 만들지 않는다** (비용·불안정)
- LLM 응답 파싱 테스트는 최소 4종을 포함한다:
  정상 JSON / 코드펜스 / JSON 앞 평문 접두사 / 완전 파손
